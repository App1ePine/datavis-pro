# DataVis Pro - Tauri 架构说明文档

## 📚 目录

1. [什么是 Tauri](#什么是-tauri)
2. [项目整体架构](#项目整体架构)
3. [前后端通信机制](#前后端通信机制)
4. [Rust 后端详解](#rust-后端详解)
5. [Vue 前端详解](#vue-前端详解)
6. [数据流程详解](#数据流程详解)
7. [关键概念解释](#关键概念解释)

---

## 什么是 Tauri

### 基本概念

Tauri 是一个用于构建桌面应用的框架，类似于 Electron，但更轻量、更快、更安全。

**核心特点：**

- **前端**：使用 Web 技术（HTML/CSS/JavaScript/Vue/React 等）
- **后端**：使用 Rust 语言（高性能、内存安全）
- **通信**：前后端通过 IPC（进程间通信）交互
- **打包**：生成原生桌面应用（Windows/macOS/Linux）

### 与 Electron 的区别

| 特性   | Tauri        | Electron   |
|------|--------------|------------|
| 后端语言 | Rust         | Node.js    |
| 包大小  | ~3-5 MB      | ~50-100 MB |
| 内存占用 | 低            | 高          |
| 启动速度 | 快            | 慢          |
| 安全性  | 高（Rust 内存安全） | 中等         |

---

## 项目整体架构

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        桌面应用窗口                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              前端（Vue 3 + TypeScript）              │   │
│  │                                                       │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │   │
│  │  │ 组件层   │  │ 状态管理 │  │ UI 组件  │          │   │
│  │  │ (Vue)    │  │ (Pinia)  │  │(Element) │          │   │
│  │  └──────────┘  └──────────┘  └──────────┘          │   │
│  │                                                       │   │
│  │  ┌──────────────────────────────────────┐          │   │
│  │  │      Tauri API (invoke 调用)         │          │   │
│  │  └──────────────────────────────────────┘          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↕                                  │
│                    IPC 通信（JSON）                          │
│                           ↕                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              后端（Rust + Tauri）                    │   │
│  │                                                       │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │   │
│  │  │ Commands │  │ 数据处理 │  │ 文件操作 │          │   │
│  │  │ (Tauri)  │  │ (Polars) │  │(Calamine)│          │   │
│  │  └──────────┘  └──────────┘  └──────────┘          │   │
│  │                                                       │   │
│  │  ┌──────────────────────────────────────┐          │   │
│  │  │   历史栈数据存储 (DataStore)         │          │   │
│  │  │   - 操作历史栈 (Vec<HistoryEntry>)   │          │   │
│  │  │   - 当前索引 (current_index)         │          │   │
│  │  │   - Undo/Redo 支持                   │          │   │
│  │  └──────────────────────────────────────┘          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 目录结构

```
datavis-pro/
├── src/                          # 前端代码（Vue 3）
│   ├── App.vue                   # 主应用组件
│   ├── main.ts                   # 前端入口
│   ├── components/               # Vue 组件
│   │   ├── Sidebar.vue          # 左侧操作面板
│   │   ├── DataGrid.vue         # 数据表格（ag-Grid）
│   │   ├── DataInfoBar.vue      # 数据信息栏
│   │   ├── RightSidebar.vue     # 右侧统计面板
│   │   └── dialogs/             # 对话框组件
│   │       ├── ExportDialog.vue        # 导出对话框
│   │       ├── FillNullDialog.vue      # 空值填充对话框
│   │       ├── FilterDialog.vue        # SQL 筛选对话框
│   │       ├── SelectColumnsDialog.vue # 选择列对话框
│   │       ├── RenameColumnsDialog.vue # 重命名列对话框
│   │       └── CastTypesDialog.vue     # 类型转换对话框
│   ├── stores/                   # Pinia 状态管理
│   │   └── dataStore.ts         # 数据状态
│   ├── types/                    # TypeScript 类型定义
│   │   ├── dataset.ts           # 数据集类型
│   │   └── history.ts           # 历史类型
│   └── utils/                    # 工具函数
│       └── tauri-commands.ts    # Tauri 命令封装
│
├── src-tauri/                    # 后端代码（Rust）
│   ├── src/
│   │   ├── main.rs              # Rust 入口
│   │   ├── lib.rs               # Tauri 应用核心
│   │   ├── error.rs             # 错误类型定义
│   │   ├── models/              # 数据模型
│   │   │   ├── mod.rs
│   │   │   ├── dataset.rs       # DatasetInfo, ColumnInfo, DatasetData
│   │   │   └── history.rs       # HistoryEntry, OperationType
│   │   ├── data/                # 数据处理
│   │   │   ├── mod.rs
│   │   │   ├── store.rs         # 历史栈数据存储
│   │   │   └── loader.rs        # CSV/TSV/Excel/Parquet 加载器
│   │   └── commands/            # Tauri 命令
│   │       ├── mod.rs
│   │       ├── file_import.rs   # 文件导入命令
│   │       ├── data_query.rs    # 数据查询命令
│   │       ├── data_export.rs   # 数据导出命令
│   │       ├── history.rs       # 历史管理命令
│   │       └── operations.rs    # 数据操作命令
│   ├── Cargo.toml               # Rust 依赖配置
│   └── tauri.conf.json          # Tauri 应用配置
│
├── TAURI架构说明.md              # 本文件
├── CLAUDE.md                     # AI 协作指南
├── 开发进度.md                   # 开发进度跟踪
├── README.md                     # 项目说明
├── package.json                  # 前端依赖配置
└── vite.config.ts               # Vite 配置
```

---

## 前后端通信机制

### IPC（进程间通信）

Tauri 使用 IPC 机制让前端和后端通信。前端运行在 WebView 中，后端运行在 Rust 进程中。

### 通信流程

```
前端（JavaScript/TypeScript）
    ↓
调用 invoke('command_name', { params })
    ↓
Tauri IPC 层（序列化为 JSON）
    ↓
Rust 后端接收命令
    ↓
执行 #[tauri::command] 函数
    ↓
返回结果（序列化为 JSON）
    ↓
Tauri IPC 层
    ↓
前端接收结果（Promise resolve）
```

### 代码示例

#### 前端调用（TypeScript）

```typescript
// src/utils/tauri-commands.ts
import {invoke} from '@tauri-apps/api/core';

// 导入 CSV 文件
export async function importCSV(filePath: string): Promise<DatasetInfo> {
    // invoke 函数：
    // - 第一个参数：命令名称（对应 Rust 函数名）
    // - 第二个参数：参数对象（会被序列化为 JSON）
    // - 返回：Promise，异步等待 Rust 返回结果
    return await invoke<DatasetInfo>('import_csv', {filePath});
}
```

#### 后端处理（Rust）

```rust
// src-tauri/src/commands/file_import.rs

// #[tauri::command] 宏：将这个函数注册为 Tauri 命令
#[tauri::command]
pub async fn import_csv(
  file_path: String,                    // 参数：文件路径
  state: tauri::State<'_, AppState>,    // 应用状态（自动注入）
) -> Result<DatasetInfo, String> {        // 返回：成功返回 DatasetInfo，失败返回错误字符串
  // 1. 读取 CSV 文件
  let df = load_csv(&file_path)
    .map_err(|e| format!("Failed to load CSV: {}", e))?;

  // 2. 生成唯一 ID
  let id = Uuid::new_v4().to_string();

  // 3. 创建数据集元信息
  let info = create_dataset_info(&id, &file_path, &df);

  // 4. 存储到 DataStore
  let mut store = state.data_store.lock()
    .map_err(|e| format!("Failed to lock data store: {}", e))?;
  store.insert(id.clone(), df, info.clone());

  // 5. 返回元信息给前端
  Ok(info)
}
```

### 数据序列化

**前端 → 后端：**

```javascript
// JavaScript 对象
{
    filePath: "/path/to/file.csv"
}
    ↓ (自动序列化)
// JSON 字符串
'{"filePath":"/path/to/file.csv"}'
    ↓ (Tauri
传输
)
// Rust 结构体
file_path: String = "/path/to/file.csv"
```

**后端 → 前端：**

```rust
// Rust 结构体
DatasetInfo { id: "uuid", name: "file.csv", ...}
↓ (自动序列化)
// JSON 字符串
'{"id": "uuid", "name": "file.csv", ...}'
↓ (Tauri 传输)
// JavaScript 对象
{ id: "uuid", name: "file.csv", ... }
```

---

## Rust 后端详解

### 核心概念

#### 1. 所有权（Ownership）

Rust 最重要的特性，确保内存安全。

```rust
// 所有权转移（Move）
let s1 = String::from("hello");
let s2 = s1;  // s1 的所有权转移给 s2，s1 不能再使用

// 借用（Borrow）
let s1 = String::from("hello");
let len = calculate_length( & s1);  // &s1 是借用，s1 仍然可用

fn calculate_length(s: &String) -> usize {
  s.len()  // 只读访问
}

// 可变借用（Mutable Borrow）
let mut s = String::from("hello");
change( & mut s);  // &mut s 是可变借用

fn change(s: &mut String) {
  s.push_str(", world");  // 可以修改
}
```

#### 2. Result 和 Option

Rust 使用这两个类型处理可能失败的操作。

```rust
// Option<T>：可能有值，也可能没有
let some_number: Option<i32> = Some(5);
let no_number: Option<i32> = None;

// Result<T, E>：操作可能成功（Ok）或失败（Err）
fn divide(a: i32, b: i32) -> Result<i32, String> {
  if b == 0 {
    Err("除数不能为零".to_string())
  } else {
    Ok(a / b)
  }
}

// 使用 ? 操作符简化错误处理
fn read_file() -> Result<String, std::io::Error> {
  let content = std::fs::read_to_string("file.txt")?;  // 如果失败，自动返回错误
  Ok(content)
}
```

#### 3. Arc 和 Mutex

用于多线程环境中安全地共享数据。

```rust
use std::sync::{Arc, Mutex};

// Arc：原子引用计数，允许多个所有者
// Mutex：互斥锁，确保同一时间只有一个线程可以访问

let data = Arc::new(Mutex::new(vec![1, 2, 3]));

// 克隆 Arc（增加引用计数，不复制数据）
let data_clone = Arc::clone( & data);

// 在另一个线程中使用
std::thread::spawn(move | | {
let mut d = data_clone.lock().unwrap();  // 获取锁
d.push(4);  // 修改数据
// 锁在 d 离开作用域时自动释放
});
```

### 模块系统

```rust
// lib.rs - 根模块
mod error;      // 声明 error 模块（对应 error.rs 文件）
mod models;     // 声明 models 模块（对应 models/ 文件夹）
mod data;       // 声明 data 模块
mod commands;   // 声明 commands 模块

// 导入模块中的内容
use data::DataStore;
use commands::import_csv;

// models/mod.rs - 子模块的入口
pub mod dataset;  // 声明 dataset 子模块（对应 dataset.rs）

// 重新导出，让外部可以直接使用
pub use dataset::{DatasetInfo, ColumnInfo};
```

### Tauri 命令注册

```rust
// lib.rs
tauri::Builder::default ()
.manage(AppState { data_store })  // 注册应用状态
.invoke_handler(tauri::generate_handler![
        import_csv,      // 注册命令
        import_excel,
        get_dataset_data,
    ])
.run(tauri::generate_context!())
.expect("error while running tauri application");
```

### 数据存储（DataStore）- 历史栈架构

```rust
// 基于历史栈的数据存储（新架构）
pub struct DataStore {
  history: Vec<HistoryEntry>,      // 操作历史栈
  current_index: Option<usize>,    // 当前历史索引
  max_history: usize,              // 最大历史深度（默认 50）
}

// 历史条目结构
pub struct HistoryEntry {
  id: String,                      // 唯一 ID
  operation: OperationType,        // 操作类型
  dataframe: DataFrame,            // 操作后的完整数据
  metadata: DatasetInfo,           // 元信息
  timestamp: String,               // 时间戳
  description: String,             // 操作描述
}

// 为什么使用历史栈？
// 1. 支持 Undo/Redo 功能
// 2. 每个历史节点存储完整状态，操作简单
// 3. 线性历史：回退后新操作会丢弃后续历史
// 4. 限制最大深度，防止内存溢出
```

---

## Vue 前端详解

### Pinia 状态管理

Pinia 是 Vue 3 的官方状态管理库，用于在组件之间共享数据。

```typescript
// stores/dataStore.ts
export const useDataStore = defineStore('data', () => {
    // 状态（State）
    const datasets = ref<Map<string, DatasetInfo>>(new Map());
    const currentDatasetId = ref<string | null>(null);
    const loading = ref(false);

    // 计算属性（Getters）
    const currentDataset = computed(() =>
        currentDatasetId.value
            ? datasets.value.get(currentDatasetId.value)
            : null
    );

    // 操作（Actions）
    async function importCSV(filePath: string) {
        loading.value = true;
        try {
            // 调用 Rust 后端
            const info = await commands.importCSV(filePath);
            // 更新状态
            datasets.value.set(info.id, info);
            currentDatasetId.value = info.id;
        } finally {
            loading.value = false;
        }
    }

    return {
        datasets,
        currentDatasetId,
        loading,
        currentDataset,
        importCSV,
    };
});
```

### 组件使用 Store

```vue

<script setup lang="ts">
  import {useDataStore} from '@/stores/dataStore';

  const dataStore = useDataStore();

  // 直接访问状态
  console.log(dataStore.loading);
  console.log(dataStore.currentDataset);

  // 调用操作
  async function handleImport() {
    await dataStore.importCSV('/path/to/file.csv');
  }
</script>

<template>
  <div>
    <p v-if="dataStore.loading">加载中...</p>
    <p v-else>{{ dataStore.currentDataset?.name }}</p>
    <button @click="handleImport">导入</button>
  </div>
</template>
```

### ag-Grid 数据表格

```vue

<script setup lang="ts">
  import {AgGridVue} from 'ag-grid-vue3';
  import {useDataStore} from '@/stores/dataStore';

  const dataStore = useDataStore();

  // 列定义
  const columnDefs = computed(() => {
    if (!dataStore.currentData) return [];
    return dataStore.currentData.columns.map(col => ({
      field: col,
      headerName: col,
      sortable: true,
      filter: true,
    }));
  });

  // 行数据
  const rowData = computed(() => {
    if (!dataStore.currentData) return [];
    return dataStore.currentData.rows.map(row => {
      const obj: any = {};
      dataStore.currentData!.columns.forEach((col, i) => {
        obj[col] = row[i];
      });
      return obj;
    });
  });
</script>

<template>
  <ag-grid-vue
      :columnDefs="columnDefs"
      :rowData="rowData"
      :pagination="true"
      :paginationPageSize="100"
  />
</template>
```

---

## 数据流程详解

### 1. 文件导入流程

```
用户操作：点击"上传数据"按钮
    ↓
前端：打开文件选择对话框
    ↓
用户选择文件：/path/to/data.csv
    ↓
前端：调用 dataStore.importCSV(filePath)
    ↓
前端：invoke('import_csv', { filePath })
    ↓
IPC 通信：JSON 序列化并传输
    ↓
Rust 后端：import_csv 函数接收请求
    ↓
Rust：使用 Polars 读取 CSV 文件
    ↓
Rust：解析为 DataFrame（内存中的表格数据）
    ↓
Rust：生成 UUID 作为数据集 ID
    ↓
Rust：提取元信息（行数、列信息、类型等）
    ↓
Rust：存储到 DataStore（HashMap）
    ↓
Rust：返回 DatasetInfo 给前端
    ↓
IPC 通信：JSON 序列化并传输
    ↓
前端：接收 DatasetInfo
    ↓
前端：更新 Pinia store
    ↓
前端：自动触发 UI 更新
    ↓
用户看到：数据集出现在列表中
```

### 2. 数据查询流程

```
用户操作：点击数据集查看数据
    ↓
前端：调用 dataStore.loadDatasetData(id, 0, 100)
    ↓
前端：invoke('get_dataset_data', { datasetId, offset: 0, limit: 100 })
    ↓
IPC 通信
    ↓
Rust 后端：get_dataset_data 函数接收请求
    ↓
Rust：从 DataStore 获取 DataFrame
    ↓
Rust：切片数据（df.slice(0, 100)）
    ↓
Rust：将 DataFrame 转换为 JSON 格式
    ↓
Rust：返回 DatasetData { columns, rows, total_rows }
    ↓
IPC 通信
    ↓
前端：接收 DatasetData
    ↓
前端：更新 Pinia store
    ↓
前端：ag-Grid 自动渲染表格
    ↓
用户看到：数据表格显示
```

### 3. 数据导出流程

```
用户操作：点击"导出数据"按钮
    ↓
前端：打开保存文件对话框
    ↓
用户选择保存位置：/path/to/output.csv
    ↓
前端：调用 dataStore.exportDataset(id, outputPath)
    ↓
前端：invoke('export_csv', { datasetId, outputPath })
    ↓
IPC 通信
    ↓
Rust 后端：export_csv 函数接收请求
    ↓
Rust：从 DataStore 获取 DataFrame
    ↓
Rust：使用 Polars 写入 CSV 文件
    ↓
Rust：返回成功消息
    ↓
IPC 通信
    ↓
前端：显示成功提示
    ↓
用户看到：文件已保存
```

---

## 关键概念解释

### 1. 为什么使用 Rust？

**优点：**

- **性能**：接近 C/C++ 的性能，比 Node.js 快得多
- **内存安全**：编译时检查，避免内存泄漏和数据竞争
- **并发安全**：类型系统保证线程安全
- **小体积**：编译后的二进制文件很小

**适用场景：**

- 数据处理（Polars 比 Pandas 快）
- 文件操作（高效的 I/O）
- 系统级操作（访问硬件、文件系统）

### 2. 为什么使用 Polars？

Polars 是一个高性能的数据处理库，类似于 Python 的 Pandas。

**优点：**

- **速度快**：使用 Rust 编写，利用 SIMD 和多线程
- **内存效率**：基于 Apache Arrow 内存格式
- **Lazy 执行**：查询优化，只计算需要的数据
- **类型安全**：编译时类型检查

**示例：**

```rust
// 读取 CSV
let df = CsvReader::from_path("data.csv") ?.finish() ?;

// 数据操作
let result = df
.lazy()                              // 转换为 LazyFrame
.filter(col("age").gt(18))          // 筛选：年龄 > 18
.group_by(vec![col("city")])        // 分组：按城市
.agg(vec![col("salary").mean()])    // 聚合：平均工资
.collect() ?;                         // 执行查询
```

### 3. 为什么使用 Arc<Mutex<T>>？

在 Tauri 中，多个命令可能同时执行，需要安全地共享数据。

```rust
// 问题：多个线程同时访问数据
let data = DataStore::new();  // ❌ 不能在多线程中共享

// 解决方案：Arc + Mutex
let data = Arc::new(Mutex::new(DataStore::new()));  // ✅ 线程安全

// 使用方式
let store = data.lock().unwrap();  // 获取锁
let df = store.get("id");          // 访问数据
// 锁自动释放
```

**Arc（Atomic Reference Counted）：**

- 允许多个所有者共享数据
- 引用计数，最后一个所有者释放时清理数据
- 线程安全的引用计数

**Mutex（Mutual Exclusion）：**

- 互斥锁，确保同一时间只有一个线程可以访问
- lock() 获取锁，返回 MutexGuard
- MutexGuard 离开作用域时自动释放锁

### 4. 为什么使用 Pinia？

Pinia 是 Vue 3 的官方状态管理库，替代 Vuex。

**优点：**

- **类型安全**：完整的 TypeScript 支持
- **简单**：API 更简洁，没有 mutations
- **模块化**：每个 store 独立
- **DevTools**：支持 Vue DevTools 调试

**使用场景：**

- 跨组件共享数据
- 管理应用状态（加载状态、错误状态等）
- 缓存数据（避免重复请求）

### 5. 为什么使用 ag-Grid？

ag-Grid 是一个企业级的数据表格组件。

**优点：**

- **性能**：虚拟滚动，可以处理百万行数据
- **功能丰富**：排序、筛选、分组、导出等
- **可定制**：支持自定义渲染、编辑器等
- **响应式**：自适应不同屏幕尺寸

**适用场景：**

- 大数据量表格展示
- 需要复杂交互（排序、筛选、编辑）
- 企业级应用

---

## 总结

### 技术栈选择理由

| 技术           | 理由                |
|--------------|-------------------|
| Tauri        | 轻量、快速、安全的桌面应用框架   |
| Rust         | 高性能、内存安全、适合数据处理   |
| Polars       | 比 Pandas 更快的数据处理库 |
| Vue 3        | 现代化、响应式的前端框架      |
| TypeScript   | 类型安全，减少运行时错误      |
| Pinia        | 简单、类型安全的状态管理      |
| ag-Grid      | 高性能、功能丰富的数据表格     |
| Element Plus | 美观、完整的 UI 组件库     |

### 数据流总结

```
文件系统 → Rust (Polars) → 内存 (DataStore) → JSON → 前端 (Pinia) → UI (ag-Grid)
```

### 关键设计决策

1. **数据存储在 Rust 后端**：性能更好，内存管理更安全
2. **前端不存储数据**：只存储元信息和当前显示的数据
3. **分页加载**：避免一次性传输大量数据
4. **类型安全**：Rust 和 TypeScript 都有强类型系统
5. **模块化设计**：前后端代码都按功能模块组织

---

## 下一步学习建议

### 如果你想深入学习 Rust：

1. **官方教程**：[The Rust Programming Language](https://doc.rust-lang.org/book/)
2. **所有权系统**：理解 Rust 最核心的概念
3. **错误处理**：Result 和 Option 的使用
4. **并发编程**：Arc、Mutex、线程安全

### 如果你想深入学习 Tauri：

1. **官方文档**：[Tauri Docs](https://tauri.app/)
2. **命令系统**：如何定义和调用命令
3. **插件系统**：使用和开发 Tauri 插件
4. **打包发布**：如何构建和分发应用

### 如果你想扩展项目功能：

1. **数据操作**：实现筛选、排序、聚合等功能
2. **数据可视化**：集成 ECharts 绘制图表
3. **持久化存储**：使用 SQLite 保存数据
4. **性能优化**：Lazy loading、虚拟滚动等

---

**文档版本**：1.0
**最后更新**：2026-01-24
**作者**：Claude (Anthropic)