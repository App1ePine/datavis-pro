// ============================================================================
// main.rs - Rust 应用程序入口文件
// ============================================================================
// 这是整个 Rust 应用的启动点，类似于其他语言的 main 函数
// 在 Tauri 应用中，main.rs 通常只负责调用 lib.rs 中的 run() 函数

// 防止在 Windows 发布版本中显示额外的控制台窗口
// 这是一个编译器属性，告诉编译器在非调试模式下使用 Windows 子系统
// 不要删除这一行！否则 Windows 用户会看到一个黑色的命令行窗口
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

/// 主函数 - 程序的入口点
///
/// 在 Rust 中，main() 函数是程序开始执行的地方
/// 这里我们只是调用 lib.rs 中定义的 run() 函数来启动 Tauri 应用
fn main() {
    // 调用库文件中的 run() 函数
    // datavis_pro_lib 是在 Cargo.toml 中定义的库名称
    // :: 是 Rust 中访问模块或类型的语法，类似于其他语言的 . 或 ->
    datavis_pro_lib::run()
}